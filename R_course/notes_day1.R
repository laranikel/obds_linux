# For help, type help(command) e.g. help (sd)

# Can run the example function of the command on the help page to see examples of variables

# Open description of packages by clicking on the name; see all fucntions within that package > can access help page


# Vectors are collections of scalars (single object variables); function: c()
# to add to a vector, create vector with old vector name and new input varaiables


# tab within functions to see optional arguments